import React from "react";
import "./App.css"; // Make sure this matches the exact case of your file
import AviationCourseDashboard from "./AviationCourseDashboard";							
ReactDOM.createRoot(document.getElementById('root')).render(
	<React.StrictMode>
		<App />
	</React.StrictMode>
)